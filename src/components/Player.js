const Player = () => {
  return (
    <footer className="fixed bottom-0 left-0 w-full bg-white border-t px-4 py-2 flex items-center justify-between">
     
    </footer>
  );
};

export default Player;
